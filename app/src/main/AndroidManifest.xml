<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Audio Recording -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Local Wi-Fi / Hotspot Socket Transport -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Bluetooth Classic & BLE Transceiver -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- Hardware / Feedback -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Offline Emergency Distress Location (Requested strictly on-demand during SEND DISTRESS) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Optical QR Code Scanner for Tactical Contact Pairing -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!--
        Foreground Service — MANET Node Mode
        FOREGROUND_SERVICE: required to call startForeground() on any API level.
        FOREGROUND_SERVICE_CONNECTED_DEVICE: required on API 34+ (Android 14+) for
          the connectedDevice foreground service type. Selected over dataSync because:
          1) Android 15 (targetSdk 35) imposes a strict 6-hour cumulative execution
             timeout on dataSync services, which would terminate an ad-hoc relay node.
          2) iTantra maintains persistent Bluetooth RFCOMM sockets (BluetoothSocket/
             BluetoothServerSocket) and Wi-Fi Multicast sockets connecting external
             peer hardware devices, fitting the connectedDevice specification.
          3) connectedDevice is not subject to the 6-hour timeout in Android 15.
        POST_NOTIFICATIONS: required on API 33+ (Android 13+) to show the
          persistent MANET node notification. Requested at runtime in MainActivity.
        RECEIVE_BOOT_COMPLETED: allows BootReceiver to detect reboot and post
          a nudge notification (does NOT start foreground service from receiver).
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            ManetNodeService — persistent MANET relay node.

            foregroundServiceType="connectedDevice":
              Selected because this service connects to and maintains communication
              with external peer hardware devices over Bluetooth RFCOMM (BluetoothSocket)
              and local Wi-Fi UDP sockets. Unlike dataSync (which is subject to a 6-hour
              cumulative timeout in Android 15), connectedDevice does not have a 6-hour
              runtime cap and accurately describes the persistent physical device link.

            stopWithTask="false":
              Service continues when MainActivity is destroyed (back-stack cleared).
              This is the key mechanism that keeps MANET alive after UI closure.

            exported="false":
              Service is private to this app; no external components can bind it.
        -->
        <service
            android:name=".service.ManetNodeService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice"
            android:stopWithTask="false" />

        <!--
            BootReceiver — posts nudge notification after reboot if Node Mode was ON.
            Does NOT start a foreground service (prohibited on Android 12+ from receiver).
            exported="true" is required for BOOT_COMPLETED to be delivered.
        -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
